<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.iiidev.pinda.authority.biz.dao.auth.ResourceMapper">

    <select id="findVisibleResource" parameterType="org.iiidev.pinda.authority.dto.auth.ResourceQueryDTO"
            resultMap="BaseResultMap">
        SELECT
            <include refid="Base_Column_List"/>
        from pd_auth_resource
        where id in (
            SELECT authority_id FROM pd_auth_role_authority ra
                JOIN pd_auth_user_role ur on ra.role_id = ur.role_id
                JOIN pd_auth_role result on result.id = ra.role_id
            where ur.user_id = #{userId, jdbcType=BIGINT} and result.`status` = true
            and ra.authority_type = 'RESOURCE'
        )
    </select>

    <select id="findMenuIdByResourceId" parameterType="map" resultType="java.lang.Long">
        SELECT DISTINCT menu_id from pd_auth_resource where id in
        <foreach close=")" collection="resourceIdList" item="id" open="(" separator=",">
            #{id}
        </foreach>
    </select>

</mapper>
